/**
 * Test data for signatures collection
 */

// Define the signature type
export interface Signature {
  user: string;
  agree_checkbox: boolean;
  public_display: boolean;
}

// Empty array for now
export const testSignatures: Signature[] = [];
